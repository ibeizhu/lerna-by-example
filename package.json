{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "build": "npx lerna run build",
    "dev": "npx lerna run dev --scope=remixapp",
    "graph": "npx nx graph",
    "publish": "npx lerna publish --no-private",
    "test": "npx lerna run test"
  },
  "workspaces": [
    "packages/*"
  ]
}
